#!/usr/bin/env python3
"""Legacy helper retained for reference.

Do not use this with the integrated Nav2/Gazebo stack because Gazebo already
publishes /odom. The integrated vehicle_node reads /odom directly.
"""

print('Legacy helper only: integrated stack uses Gazebo /odom, not /vehicle/pose.')
